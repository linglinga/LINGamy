
// File: bool.cs

using System;

class TestBool
{
    static void Main()
    {
        bool flag = true; // true or false.
                            // call the type: bool
                            // in blue are C# functions.                    
                            // flag is a name, could be any name.
                            // assign a boolean type: true.
                            
                            // flag is assigned true.
        flag = false;

        int x = 1; // declared and initialized before using them.
        // if change to x = 2; then flag = (x == 1); will be false.

        // Assign flag to true or false
        // depending on whether x is
        // equal to 1.
        flag = (x == 1);    // x == 1 means: x and 1 are the same. result is a boolean value. a compare.
                             // boolean value can only be stored in boolean.
                             // so assign flag to a boolean statement.
                           // x = 1 means: x is assigned to 1.
    }
}